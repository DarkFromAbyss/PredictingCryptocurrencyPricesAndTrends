# --- Web Framework ---
fastapi==0.115.0
uvicorn[standard]==0.32.0
python-dotenv==1.0.1
pydantic==2.9.2
pydantic-settings==2.6.0

# --- Networking ---
httpx==0.27.0           # Client để gọi sang Data Engine và ML Brain (Quan trọng)

# --- Database (PostgreSQL) ---
sqlalchemy==2.0.36      # ORM để thao tác DB bằng code Python
asyncpg==0.30.0         # Driver kết nối Postgres bất đồng bộ (Nhanh nhất cho Python)
alembic==1.13.3         # Quản lý version cho Database (Migration tool)

# --- Security (Optional - Nếu bạn muốn làm đăng nhập sau này) ---
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4